# Kleegr Voice Comments

Mic button for GoHighLevel internal comments. Records a voice note in the browser, uploads it, and posts it as an `InternalComment` with the audio attached. Standalone Next.js app on Vercel — no dependency on the WhatsApp droplet. See full docs below (updated by subsequent commit).
